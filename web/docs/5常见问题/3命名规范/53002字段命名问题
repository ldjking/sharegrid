         目前平台中的表的字段命名为表的前缀+'_'+字段名，字段名不能准确反映界面字段，而且有些字段存储的
数据格式不能反映界面字段信息，如人员字段的存储格式为数字，在程序维护或程序测试过程中，得通过查询
查询人员表才能得到人员编码所代表的的那个人的姓名。
建议：
1、表的字段名要能反映准确的信息，开发人员或维护人员根据字段名就能判断该字段在界面上的位置，
反过来通过界面上的字段信息可以在数据库表中迅速找到那个字段。
2、数据的存储格式：人员编码建议存储为拼音形式，状态值也存储为字母形式（用户可以通过状态值大致知道状态含义）
