目前快速开发平台界面显示存在如下问题：
1、用户进入相关记录后，无法区分哪些信息可以编辑，哪些信息不可编辑。
2、主子表显示在同一界面，当子表为空时，使用人员无法区分哪个是主表信息，哪个是子表信息。
3、一个界面上不能支持上方显示主表信息、下方为多项子表的标签页的形式。
4、不支持主表、子表信息混排的显示形式。
5、平台中新增记录只能在列表中增加，如果在界面停留在表单上，需切换到列表界面才能新建。